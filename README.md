## 3D In-Vitro Lead Scoring Dashboard

A Python + Streamlit based prototype that demonstrates identification,
enrichment, and propensity-based ranking of biotech professionals.

### Run Locally
pip install -r requirements.txt
python -m streamlit run app.py
